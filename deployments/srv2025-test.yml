os: windows-server-2025
vm_name: srv2025-test

# output_dir: artifacts/srv2025-test

# Static IP in CIDR notation — comment out for DHCP
static_ip: 192.0.2.61/24
gateway: 192.0.2.1
dns: 192.0.2.1

username: stu
password: MySecretPass1!

# Windows-specific (ignored for AlmaLinux):
# win_ssh_public_key: "ssh-ed25519 AAAA..."   # overrides .ssh_public_key file lookup
