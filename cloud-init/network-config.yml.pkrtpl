version: 2
ethernets:
  eth0:
%{ if static_ip != "" ~}
    dhcp4: false
    addresses:
      - ${static_ip}
    gateway4: ${gateway}
    nameservers:
      addresses: [${dns}]
%{ else ~}
    dhcp4: true
%{ endif ~}
